<div class="footer">
    Nové příspěvky jsou dostupné ihned na<br>
    <a href="https://www.instagram.com/obchoduj.na.krizikovi" target="_blank">@obchoduj.na.krizikovi</a>
</div>
<div class="dark-mode-toggle" title="Zapnout Tmavý režim">🌙</div>
<script>
    const btn = document.querySelector('div.dark-mode-toggle');
    const body = document.body;
    const cards = document.querySelectorAll('a.card');
    const footer = document.querySelector('div.footer');
    const links = document.querySelectorAll('a');

    function toggleDarkMode() {
        btn.classList.toggle('dark-mode');
        body.classList.toggle('dark-mode');
        footer.classList.toggle('dark-mode');

        for (const card of cards)
            card.classList.toggle('dark-mode');

        if (btn.classList.contains('dark-mode')) {
            btn.textContent = '☀️';
            btn.setAttribute('title', 'Zapnout Světlý režim');
            setDarkModeState('true');

        } else {
            btn.textContent = '🌙';
            btn.setAttribute('title', 'Zapnout Tmavý režim');
            setDarkModeState('false');
        }
    }

    if (localStorage.getItem('isDarkModeEnabled') == null)
        localStorage.setItem('isDarkModeEnabled', 'false');

    function setDarkModeState(state) {
        localStorage.setItem('isDarkModeEnabled', state);
        
        return;
    }

    function getDarkModeState() {
        return localStorage.getItem('isDarkModeEnabled');
    }
    
    getDarkModeState() == 'true' && toggleDarkMode();

    btn.addEventListener('click', toggleDarkMode);
</script>
</body>
</html>
